import * as Student from "../../../services/student/controllers/student.controller";

describe('student.controller.ts', () => {
    describe('createStudent', () => {
        test('should ', () => {
        });
    });
});